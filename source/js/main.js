/* jshint undef: true, unused: true, browser: true, esversion: 6, devel: true */

window.onload = () => {
	let intro = document.querySelector('.intro');
	let canvas = document.querySelector('canvas');
	intro.classList.add( 'loaded' );
	canvas.classList.add( 'loaded' );
};

